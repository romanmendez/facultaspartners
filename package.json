{
  "name": "facultas-partners",
  "version": "1.0.0",
  "description": "Landing page for the company",
  "author": "Román Méndez Beck",
  "license": "ISC",
  "scripts": {
    "start": "parcel index.html",
    "test": "jest --watch",
    "build": "parcel build index.html",
    "deploy": "npm run build && scp -i ~/.ssh/digital_ocean -r ./dist/* romanmendez@46.101.171.98:/var/www/facultaspartners.com/html",
    "lint": "eslint --ignore-path .gitignore --ext .js,.ts,.tsx .",
    "check-types": "tsc",
    "prettier": "prettier --ignore-path .gitignore \"**/*.+(js|json|ts|tsx)\"",
    "format": "npm run prettier -- --write",
    "check-format": "npm run prettier -- --check",
    "validate": "npm-run-all --parallel check-types check-format lint build"
  },
  "lint-staged": {
    "**/*.+(js|json|css|html|md)": [
      "prettier"
    ]
  },
  "dependencies": {
    "@hookform/error-message": "0.0.2",
    "@material-ui/core": "^4.11.0",
    "emailjs-com": "^2.4.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-hook-form": "^6.0.1",
    "react-scroll": "^1.7.16",
    "styled-components": "^5.1.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.11.0",
    "eslint": "^6.6.0",
    "jest": "^26.0.1",
    "lint-staged": "^10.2.7",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.9.3",
    "prettier": "^1.19.1",
    "react-scripts": "^5.0.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
